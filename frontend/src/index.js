import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <p>Hello World!</p>
    <p>Don't forget to add your metadata on index.html and manifest.json</p>
  </React.StrictMode>,
  document.getElementById('root')
);
